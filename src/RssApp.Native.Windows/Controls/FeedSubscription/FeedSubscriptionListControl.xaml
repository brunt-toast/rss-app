<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="RssApp.Native.Windows.Controls.FeedSubscription.FeedSubscriptionListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RssApp.Native.Windows.Controls.FeedSubscription"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:feedSubscription="using:RssApp.Application.Models.FeedSubscription"
    mc:Ignorable="d"
    x:Name="Root">

    <ListView ItemsSource="{x:Bind ViewModel.Feeds}" SelectionMode="None">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="feedSubscription:FeedSubscriptionModel">
                <Grid ColumnDefinitions="*,auto,auto">
                    <HyperlinkButton Grid.Column="0" Content="{x:Bind FeedName}" />
                    <Button Grid.Column="1" Command="{x:Bind RequestEditCommand}">
                        <FontIcon Glyph="&#xE70F;" />
                    </Button>
                    <Button Grid.Column="2" Command="{x:Bind RequestDeleteCommand}">
                        <FontIcon Glyph="&#xE74D;" />
                    </Button>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
